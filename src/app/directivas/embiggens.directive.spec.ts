import { EmbiggensDirective } from './embiggens.directive';

describe('EmbiggensDirective', () => {
  it('should create an instance', () => {
    const directive = new EmbiggensDirective();
    expect(directive).toBeTruthy();
  });
});
