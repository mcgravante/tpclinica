import { FiltroEspecialidadesPipe } from './filtro-especialidades.pipe';

describe('FiltroEspecialidadesPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroEspecialidadesPipe();
    expect(pipe).toBeTruthy();
  });
});
