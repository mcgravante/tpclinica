import { FiltroTurnoPipe } from './filtro-turno.pipe';

describe('FiltroTurnoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroTurnoPipe();
    expect(pipe).toBeTruthy();
  });
});
