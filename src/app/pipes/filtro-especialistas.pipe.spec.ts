import { FiltroEspecialistasPipe } from './filtro-especialistas.pipe';

describe('FiltroEspecialistasPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroEspecialistasPipe();
    expect(pipe).toBeTruthy();
  });
});
