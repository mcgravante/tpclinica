import { FiltroMixtoEspecialidadPacientePipe } from './filtro-mixto-especialidad-paciente.pipe';

describe('FiltroMixtoEspecialidadPacientePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroMixtoEspecialidadPacientePipe();
    expect(pipe).toBeTruthy();
  });
});
